{
  "private": true,
  "scripts": {
    "test-sass": "obt build --sass=test-sass/test.scss --buildCss=test.css && mocha test-sass/assets.test.js"
  },
  "devDependencies": {
    "mocha": "^2.2.1",
    "proclaim": "^3.5.0",
    "sass-true": "^3.0.2"
  }
}
